#include<stdio.h>
int main()
{
int num,count=0,i;
printf("enter num");
scanf("%d",&num);
for(i=2;i<n;i++)
{
 if(num%2==0)
   count++;
 }
 if(count==0)
   printf("prime num");
   else
    printf("not prime");
  return 0;
 }
   
